interface:
  display_name: "Word Control"
  short_description: "Safely edit the active Word document in place"
  default_prompt: "Use $word-control to make the requested narrow edit in the active Word document, following the workflow's backup policy, fresh target guards and affected-scope verification."
